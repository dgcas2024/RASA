version: "3.1"

flows:
  main_flow:
    description: "Nhắc nợ khách hàng"
    always_include_in_prompt: true
    nlu_trigger:
    - intent: "intent_start"
    steps:
    - collect: result_step1_xac_dinh_khach_hang
      # ask_before_filling: true -> khong can, do chi xac dinh 1 lan
      # utter: utter_ask_result_step1_xac_dinh_khach_hang --> auto